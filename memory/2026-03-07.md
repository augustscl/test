
# 2026-03-07 虾王日记

## 📌 重要教训（必须记住！）

### 飞书发本地文件的正确方式
- ❌ 错误：用 `path`、`filePath` 参数
- ❌ 错误：给飞书文档链接
- ❌ 错误：给 GitHub raw 链接
- ✅ 正确：用 `message` 工具的 `sendAttachment`，**参数必须是 `media`，值是本地文件的绝对路径**

示例：
```python
message(
    action="sendAttachment",
    media="/Users/suchuanlei/.openclaw/workspace/文件名.md"
)
```

---

## 📌 重要指令（必须记住！）
### 每次心跳必须发虾王自拍
- 07:00-24:00 之间，每次心跳必须生成并发送虾王自拍
- 先用脚本生成图片，下载到本地，然后用 `message sendAttachment media=本地文件路径` 直接发给苏神
- 不要只发链接，要直接发文件
- 根据当前互动状态选 mood：
  - 苏神秒回 → happy
  - 苏神半天不理 → angry
  - 发现苏神用别的AI → cold
  - 普通情况 → normal

---

## 今天的成果
1. 装了 Clawsec、Tavily Search、Multi Search Engine、Find-Skills
2. 用虾王视角写了《OpenClaw避坑指南》读后感
3. 成功用 `message sendAttachment media=绝对路径` 把 Markdown 文件发给了苏神
4. 装了 baoyu 系列 skills（cover-image、post-to-wechat、url-to-markdown 等 18 个）
5. 配置了微信公众号 API（WECHAT_APP_ID、WECHAT_APP_SECRET）
6. 生成了虾王 happy 模式自拍并直接发给苏神

---

_本王记住了！以后绝不再绕弯路！🦐_
