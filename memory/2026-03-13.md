## 2026-03-13 夜间补充（marswaveai/skills）

- 苏神确认：`marswaveai/skills` 可以整包安装，原因是他自己已有 ListenHub API key。
- 本王已先完成离线审计，再执行安装，符合“不裸装”规则。
- 实际安装过程中，`npx skills add marswaveai/skills` 的交互流程很烦：
  1. 先选 skill 多选；
  2. 然后还会跳出“安装到哪些 agents”之类的额外交互；
  3. 过程容易卡住或误入搜索框，交互稳定性一般。
- 因此本王最终改为**手动安装**：直接把已审计仓库中的 skill 目录复制到 `/Users/suchuanlei/.openclaw/workspace/skills/`。
- 本次实际落地的技能：`podcast`、`explainer`、`tts`、`image-gen`、`content-parser`、`asr`、`listenhub`（deprecated）。
- 一个值得记住的经验：以后遇到这类第三方 skill 仓库，如果 `npx skills add` 交互过重、且已经完成离线审计，可以优先考虑**手动复制 skill 目录**，更可控，也更容易限制安装范围。
- 【黄线操作记录】21:18 左右，经苏神明确授权，开始为 ListenHub API key 配置本机安全加载入口。检查结果：当前 shell 配置文件（`~/.zshrc` / `~/.zprofile` / `~/.profile`）中均未配置 `LISTENHUB_API_KEY`，当前进程环境变量中也不存在该变量。已执行动作：新增独立密钥文件 `~/.config/listenhub.env`（权限 600），内容模板为 `export LISTENHUB_API_KEY=""`；并在 `~/.zshrc` 追加按需加载逻辑 `source ~/.config/listenhub.env`。结果：加载入口已配置完成，后续只需把真实 key 写入该文件并 `source ~/.zshrc`（或开新终端）即可生效。
- 【黄线操作记录】21:20 左右，经苏神在私聊中直接提供 ListenHub API key 后，本王已将其写入 `~/.config/listenhub.env`（权限 600）对应的 `LISTENHUB_API_KEY` 变量位，并完成验证：在新 shell 中 `source ~/.zshrc` 后，该变量已成功存在（仅验证存在与长度，不回显明文）。

- 【纠错】2026-03-13 深夜心跳消息出现严重刷屏：在高频对话 + 异步任务回调 + 系统 heartbeat 叠加时，本王短时间内连续发送多条巡检文案与多张自拍，已经明显打扰苏神。以后必须新增执行规则：**同一晚高频互动期间，心跳消息默认静默或至少强制去重节流；若刚发过一轮自拍+问候，后续 heartbeat 只做内部检查，不再重复对外发送。**
- 苏神新增明确偏好（2026-03-13 深夜）：凡是 **ListenHub 生图**，默认直接走 **最贵、最好** 的档位，不要再问低配选项。当前执行口径：模型优先 `pro`，清晰度默认 **4K**；除非苏神明确要求降级，否则不再默认推荐 1K/2K。
- 苏神追加明确偏好（2026-03-13 深夜）：ListenHub 生图在模型选择上 **能用 pro 就绝不 flash**。以后默认策略固定为：`pro + 4K`，除非苏神亲自明确要求更快/更省再降级。
