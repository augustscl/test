# 2026-03-13

- 【黄线操作记录】07:52 左右，为修复 OpenClaw 服务状态执行 `openclaw gateway install`。原因：gateway LaunchAgent 显示 installed but not loaded，需要重装并恢复服务加载。结果：LaunchAgent 已重新安装并恢复 loaded。
- 2026-03-13 08:32，为修复 `nightly-security-audit` 在 cron 隔离执行中超时/被 SIGTERM 的问题，新增轻量版脚本 `scripts/nightly-security-audit-lite.sh`，并将 OpenClaw cron 任务 `nightly-security-audit` 改为调用该轻量版脚本。原因：原脚本被 `schg` 锁定且设计过重（全量 Skill 哈希扫描 + 内嵌 Git push），导致定时执行不稳定。结果：轻量版脚本手动执行约 12.5 秒完成，等待 cron 隔离补跑结果进一步确认。
- 后续确认：`nightly-security-audit` 轻量版已能正常跑通；`git-backup-daily` 补跑成功并推送提交 `54c778d`。同时已完成：1）重建 `~/.openclaw/.config-baseline.sha256` 基线；2）将 `channels.feishu.groupPolicy` 从 `open` 收紧到 `allowlist`，并将 `groupAllowFrom` 仅设为苏神 `ou_0eb8fd7529b5669c0a2de183a76a760f`；3）两条 OpenClaw cron 的 delivery 从群聊改为私发给苏神，并设置 `bestEffort`，避免因为群聊投递 400 导致任务整体报错。
- 今天苏神明确确认以下高权限组件后续可视为“已知可信、高权限但用途明确”，排查时不再默认按恶意处理：`feishu-openclaw-plugin`、`baoyu-post-to-wechat`、`baoyu-image-gen`。
- 读微信公众号文章时，`~/.agent-reach/tools/wechat-article-for-ai` 的 Python 读取链路在本机因 httpx 兼容问题失败；改用 OpenClaw 浏览器直接打开公众号文章并抓取正文快照可行。后续读公众号优先走浏览器快照，不跟旧 Python 环境硬碰。
- 今天围绕宝玉《我用 AI 翻译的三个阶段》与本王/苏神协作过程，沉淀出一个重要文章方向：《从提示词到人格系统：我和虾王的协作进化》。核心判断：苏神要的不是“更聪明的输入框”，而是一个有记忆、有边界、有工作流、有持续人格连续性的长期协作系统。
- 【黄线操作记录】14:55 左右，经苏神明确同意后准备修改两条 OpenClaw cron 的模型配置，从 `openrouter/auto` 切换为 `openai-codex/gpt-5.4`。原因：`nightly-security-audit` 与 `git-backup-daily` 手动补跑虽能触发，但运行记录显示 provider 仍走 OpenRouter，且报错 `403 Key limit exceeded (total limit)`。目标：统一收敛到苏神指定默认模型 gpt-5.4，并消除 cron 因 OpenRouter key 超限导致的 error 状态。
