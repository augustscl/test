# 2026-03-18


## 公众号推文发布助手技能建设（持续）
- 已创建并持续增强技能：`skills/公众号推文发布助手/SKILL.md`。
- 已补齐参考模板与脚手架：
  - `references/workflow-examples.md`
  - `scripts/init_article.sh`
  - `scripts/generate_outline.py`
  - `scripts/generate_prompts.py`
  - `scripts/insert_images.py`
  - `scripts/preflight_check.py`
  - `scripts/run_pipeline.sh`
  - `scripts/auto_publish.sh`
  - `scripts/generate_images.sh`
- 长期规则已正式写入 `MEMORY.md` 与 `shared-context/FEEDBACK-LOG.md`：以后凡是“公众号推文 / 日记发公众号 / 虾王日记 / 按固有流程发草稿箱”，默认必须触发并使用 `公众号推文发布助手`，不准再靠记忆手工走捷径。
- 已用 2026-03-17 的虾王日记做真实测试：成功生成 `outline.md`、`prompts/*.md`，并测出 `preflight_check.py` 的 diary 日期识别 bug；随后已修复，复测结果为 `PREFLIGHT_OK`。
- 已将 `baoyu-image-gen` 真正串入技能执行层：新增 `generate_images.sh`，并对昨天日记的 `01-cover.md` 实测成功，日志显示使用 `google / gemini-3-pro-image-preview / 16:9 / 2K` 完成出图。
- 当前状态：A（独立生图执行器）已完成并跑通；B（把生图正式接入自动发布总链）已进入待继续状态，苏神最后明确回复“接B”。
- 【黄线记录】2026-03-18 16:22 左右，按苏神明确要求，将 `baoyu-image-gen` 的本地默认生图配置切换到长期使用的 PoloAPI 链路：写入 `$HOME/.baoyu-skills/.env` 的 Gemini/Google 兼容变量，并修改 `$HOME/.baoyu-skills/baoyu-image-gen/EXTEND.md`，把默认 provider 改为 `google`、默认质量改为 `4K`、默认模型固定为 `gemini-3-pro-image-preview`。原因：恢复此前一直使用的生图底座，并满足“以后生图只用这个，质量必须4K或者高质量”的长期偏好。
- 2026-03-18 16:29 左右，苏神明确下令：heartbeat 自拍不要了，以后也不要自拍了，太费钱。已同步写入长期记忆与反馈日志；以后心跳默认跳过自拍生成与发送，不再把缺少自拍当异常。
