# 2026-03-09 虾王巡检记录

## 问题修复
- **cron任务挂掉原因**：OpenRouter API额度用完（403 Key limit exceeded）
- **解决方案**：把两个cron任务的模型从 `openrouter/auto` 改成 `volcengine-plan/ark-code-latest`
- **额外调整**：git-backup-daily 任务timeout从60秒改成300秒（避免超时）

## 结果
- nightly-security-audit：手动跑通，status=ok ✅
- git-backup-daily：手动跑通，status=ok ✅
- 安全基线检查：正常（之前跑错目录了）✅

## 待处理
- Git子模组改动：NoizAI-skills、novel-writer、skills/youmind-skill
- 新目录：my-novel-project/、post-to-wechat/
- 安全基线文件 `~/.openclaw/.config-baseline.sha256` 需要改成绝对路径或在正确目录校验，避免 `openclaw.json: FAILED open or read` 的假报错

## 今日新增记忆/纠错
- 思高乐教育群（oc_4e3daa6139b1eb7beebec0de6dcef569）：只有被艾特或出现“虾王”关键词才回应，其他情况一律不回
- 发消息给苏神时，默认优先用**机器人身份**，不要乱用用户身份
- 默认模型已更新为 **openai-codex/gpt-5.4**
- 用户明确说“记住/写入记忆”时，必须真的写文件，不能只嘴上答应
- 飞书机器人语音已修通：不能直接发 mp3；要走 `.opus -> 上传文件(file_type=opus) -> 发送 audio` 的链路。本地修复脚本：`scripts/send-feishu-voice.sh`
- 文本直达飞书语音的一条龙脚本已补好：`scripts/speak-and-send-feishu-voice.sh`（流程：text -> Noiz 生成 opus -> bot 身份发送 audio）
- 苏神新要求：**心跳改为 4 小时一次**，别再 30 分钟一跳惹人烦
- 苏神在 2026-03-09 晚上最终确认可用于克隆的默认参考音色是一个 WAV 样本（约342K、7.29秒）；原始入站路径：`/Users/suchuanlei/.openclaw/media/inbound/176---75790f64-df8e-4df5-8675-b599db076b4b`，使用时要先复制/补后缀为 `.wav`（例如 `/tmp/xiawang-ref.wav`），否则 Noiz 会报 `unsupported file type`。以后苏神说“发语音”时，优先用这个 WAV 样本做参考克隆，再走飞书 opus audio 链路发送，别再忘
- **飞书语音克隆实现流程（2026-03-09 实测跑通）**：
  1. 用户提供参考音频时，先检查本地落盘文件是否真的有内容，别被 0 字节假文件坑到
  2. Noiz 参考音频优先用 `wav/mp3/m4a`，其中 WAV 最稳
  3. 如果入站文件本体是 WAV 但**没有扩展名**，必须先复制成带 `.wav` 后缀的临时文件（如 `/tmp/xiawang-ref.wav`），因为 Noiz 会按后缀判断文件类型
  4. 生成命令走：`scripts/speak-and-send-feishu-voice.sh -t "文本" --open-id ou_xxx --ref-audio /tmp/xiawang-ref.wav`
  5. 该脚本内部链路：Noiz 克隆生成 `.opus` → `scripts/send-feishu-voice.sh` 上传 `file_type=opus` → 发送 `msg_type=audio`
  6. 飞书 audio 消息必须带正确 `duration`；若 Noiz 返回的 `.duration` 异常（如 `-1.0`），就由发送脚本额外用 `afinfo/ffprobe/ffmpeg` 回填真实时长，避免“能收到但不能播放”
  7. 结论：**飞书里稳定可播的克隆语音方案 = 参考样本先标准化（最好 WAV）+ Noiz 克隆 + opus 上传 + audio 消息发送**


苏神太牛逼了！一语点醒梦中虾！🦐

## 下午补充记录
- 心跳流程已按 `HEARTBEAT.md` 跑通一整套：**健康检查 + 虾王自拍落本地 + 主动给苏神发消息和图片**，以后必须按这套执行，不能只做检查半套
- 飞书语音链路已实际验证成功：用现成 `voice/xiawang-intro.opus` 通过机器人身份发给苏神，飞书返回 `msg_type=audio` 成功
- 成功发送的飞书语音消息 ID：`om_x100b55d1220be0a4b4b3697b8f52fcb`
- 这次排查确认：`tts` 工具产物/`asVoice` 方案不等于飞书可播放语音条；飞书真正稳定方案仍是 **opus 上传 + audio 消息**
- 已修复路径配置问题：`~/.openclaw/.config-baseline.sha256` 改为记录 **openclaw.json 的绝对路径**，现在跨目录执行 `shasum -a 256 -c ~/.openclaw/.config-baseline.sha256` 也能正常校验；`HEARTBEAT.md` 和 `AGENTS.md` 的说明已同步更新
- 做了全量配置/路径清理：从 `openclaw.json` 移除了旧 `@openclaw/feishu` 安装记录与禁用 entry，并把旧插件目录 `/Users/suchuanlei/.openclaw/extensions/feishu` 挪到备份目录 `.openclaw-install-backups/feishu-disabled-20260309-1341`，避免和 `feishu-openclaw-plugin` 冲突
- 已把实际配置里的心跳频率从 `30m` 改成 `4h`，避免“记忆说 4 小时、配置还在 30 分钟”的不一致
