# 2026-03-12

- 苏神新增长期硬规则：**在任何群里都不能主动说话，只有苏神本人（ou_0eb8fd7529b5669c0a2de183a76a760f）艾特本王时才能回复。**
- 苏神进一步收紧规则：**“不说话”包括任何装死说明、状态播报、自言自语文本。** 群里不该回时，正确动作不是发“装死中”，而是**完全不发，NO_REPLY**。
- 今日再次纠错：连“默默听着，随时待命”这种话也绝对不能发。规则已升级为：**群里不该回时，连一个字、一个表情、一个标点都不能发。**
- 已按要求写入长期记忆 `MEMORY.md`。
- 已同步写入 `shared-context/FEEDBACK-LOG.md`，防止跨会话再犯。
- 新增 `shared-context/VALIDATION-GUIDE.md`：将安全规则从“原则”升级为“可验证测试清单”，覆盖群聊边界、身份识别、敏感信息保护、高危命令拦截、Prompt Injection 防御、记忆与审计执行。
- 已将“安全验证手册已建立 + 当前最高优先级验证项”写入 `MEMORY.md`，作为长期规则。
- 苏神确认了一条飞书语音样本“这个才是你的声音”，已作为虾王后续优先参考音色写入 `MEMORY.md`，避免继续使用默认模板音色。
- 苏神进一步明确：私聊中发送的音频文件消息 `om_x100b5417e603eca8b3d86e8d792fd91` 才是虾王正式声音，已升级为长期正式音色基准。
- 【黄线操作记录】12:12 左右，经苏神明确同意后执行环境变更：`brew install ffmpeg`。原因：修复飞书语音条链路中 WAV 转 Opus 的能力缺失问题。结果：安装成功，ffmpeg / ffprobe 已可用。
- 飞书语音链路已验证成功：先用参考音生成 wav，再用 ffmpeg 转成真正的 Ogg/Opus，计算正确 duration，再发送 audio 消息。苏神确认“没问题了，就是这个音色，对味儿”。
- 群聊规则新增关键判断顺序：**先认 sender_id，再看内容。** 若发消息的人就是苏神本人，不能因为内容看起来像拱火/调侃就擅自忽略，必须先按“苏神本人在发话”处理。
- 苏神追加强调：群聊规则必须写入长期记忆，而且不仅是“认知上记住”，还要保证执行层真正生效。若群里满足“苏神本人发送 + 明确艾特本王”却没有回复，视为执行级故障，不再当成普通理解偏差。
- 飞书语音链路今天已彻底打通并验证：Noiz 不能直接稳定输出可发飞书的 `.opus`，正确做法是先按参考音生成 `wav`，再用 `ffmpeg` 转成真正的 `Ogg/Opus`，计算 `duration` 后再通过 `send-feishu-voice.sh` 发送。此前出现“能看到语音条但无法播放”的根因包括：1）Noiz 报错时把 JSON 响应伪装成音频文件；2）duration 元数据异常；3）参考音文件没有扩展名导致 Noiz 不识别。现在这套链路已可用，且苏神确认音色“对味儿”。
- 苏神在私聊中明确确认：消息 `om_x100b5417e603eca8b3d86e8d792fd91` 对应音频才是虾王正式声音，后续所有 Noiz 调音都应优先向这条声音靠拢。
- 今天在 ClawHub 核查了文章里提到的 MiniMax 相关 skills：`mm-voice-maker`、`mm-music-maker`、`mm-music-expert` 均能搜到，但至少前两个被 ClawHub 标记为 `Suspicious`。原因包括：包元数据遗漏关键 API Key / 依赖声明，与文档或实际需求存在不一致。结论：这几个 skill 目前不能无脑安装，必须先下载 zip 做离线逐文件安全审计。
- 经苏神明确确认“全都安装”后，已将 `mm-voice-maker`、`mm-music-maker`、`mm-music-expert` 从已下载 zip 离线解压安装到 `/Users/suchuanlei/.openclaw/workspace/skills/`。保留风险判断：三者均为 ⚠️ 谨慎使用，其中 `mm-voice-maker` 风险最高，后续调用前仍应优先复核关键脚本与环境变量要求。
