# 2026-03-21

- 与苏神一起排查并接入 `ppt-afp` 技能：已将技能落地到 `workspace/skills/ppt-afp/SKILL.md`。
- 按苏神明确要求，对 `ppt-afp` 仅做了环境适配修改：
  - 飞书接收 ID 改为苏神本人 `ou_0eb8fd7529b5669c0a2de183a76a760f`
  - 默认产出目录改到 `~/.openclaw/workspace/slide-deck/{topic-slug}/` 与 `~/.openclaw/workspace/output/{主题名称}.pptx`
  - 依赖路径改到本机真实位置：`~/.openclaw/workspace/skills/baoyu-slide-deck/...` 与 `~/.openclaw/workspace/skills/baoyu-image-gen/...`
- 对 `ppt-afp` 做过真链路测试：
  - PPTX 打包成功
  - 飞书发送成功
  - AI 生图失败，错误为上游 Google/PoloAPI 通道返回 `model_not_found` / `No available channel for model ... under group nano`。
- 已确认当前问题不在本地 API key / base_url 基础配置：
  - `GOOGLE_BASE_URL=https://work.poloapi.com/` 可通
  - `GEMINI_API_KEY` 可用
  - `/v1beta/models` 返回 200，且能列出 `gemini-3-pro-image-preview`、`gemini-3.1-flash-image-preview` 等模型
  - 当前更像是上游 distributor/channel 路由问题，而非本地 skill 配置错误。
- 苏神正式要求启动 `ppt-afp` 做书籍分享 PPT，当前已确认的 P0 信息：
  - 只针对《人比 AI 凶》**第五章**内容做 PPT
  - 用途：**内地管理层读书会分享**
  - 方向：**A+B 混合**（既做章节解读，也做观点提炼）
- 处理用户发来的 PDF 时确认：该 PDF 为 **409 页扫描件**，来自复印/扫描设备（`ApeosPort-IV C7780`），基本不是可直接抽文本的文字版 PDF；普通抽取失败属于文档形态问题，不是简单读取失误。
- 2026-03-21：用 `ppt-afp` 为苏神做《浅析 OpenClaw AFP：以内容创作 IPO 为例》PPT 时，验证出一整套关键事实：
  - 最初那次“P3 正在做”的说法没有文件证据支撑；实际只有 smoke test 落地，正式项目文件并未生成。以后汇报进度前必须先查本地文件/产物，不准空口报阶段。
  - `ppt-afp` 当前技能文档里的生图命令已经过期：`baoyu-image-gen` **不支持** `--batchfile`，正确做法是按 `--promptfiles <单页prompt.md> --image <输出图>` 逐张调用，必要时自己循环批量跑。
  - 生图失败的关键不只是模型名，而是 **API/base_url/通道配置是否真的生效**。之前报 `model_not_found / No available channel ... under group nano`，在苏神手动提供并显式注入 `GOOGLE_API_KEY` + `GOOGLE_BASE_URL=https://work.poloapi.com/` + `model=gemini-3.1-flash-image-preview` 后，链路跑通。
  - 成功链路已验证：`baoyu-image-gen` + `https://work.poloapi.com/` + `gemini-3.1-flash-image-preview` 能稳定生成 12 页 PPT 图片。以后同类任务可优先用这组配置排障。
  - `merge-to-pptx.ts` 有输出命名坑：有时会在当前目录生成名为 `..pptx` 的文件；不能想当然去别处找，必须 `find` 确认真实路径。
  - 飞书文件发送有大小上限：66MB 的高清 PPTX 上传失败，返回 `234006 The file size exceed the max value.`。保底交付方案已验证：把 PNG 批量压成 1920 宽 JPEG（quality≈72）后再打包，可把 12 页 PPT 压到约 **1.9MB**，并成功通过 `send-feishu-file.sh` 发给苏神。
  - 第二轮从头重跑时，苏神明确纠正：**“从头来”包括风格也要重选**，不能默认继承上一版的风格和结构。以后用户说“重新调用 AFP skill 再做一版”，默认理解为 P0/P1 都要重新走，除非用户明确说沿用原风格。
  - 苏神第二轮风格选择：编号 **4**，气质目标是面向“智能体发烧友社群和智能体学习圈友”的 **magazine-editorial-light**。但本地 `baoyu-slide-deck` 并没有对应的 `magazine-editorial-light.md` 风格文件；以后若风格图鉴编号存在但本地 style preset 缺失，必须如实说明，并用手工 `STYLE_INSTRUCTIONS` 做风格映射，不能假装本地有这个 preset。
  - 2026-03-21 苏神明确要求：以后 `ppt-afp` 的生图调用参数要改成实测可用链路，不能再沿用旧参数。默认改为：显式注入 `GOOGLE_API_KEY` + `GOOGLE_BASE_URL=https://work.poloapi.com/`，模型优先 `gemini-3.1-flash-image-preview`，并改成逐张 `--promptfiles ... --image ...` 调用，不再使用失效的 `--batchfile`。
- 2026-03-21：继续排查 heartbeat 误报与 git 备份超时后，已做两项实修：
  - 将 `~/.openclaw/.config-baseline.sha256` 改为**绝对路径版**，并补入 `openclaw.json` 与 `devices/paired.json` 两个文件的 hash，避免以后因当前工作目录不同而误报“文件读不到”。
  - 将 `~/.openclaw/cron/jobs.json` 中 `git-backup-daily` 的 agentTurn payload 调整为：输出只保留原始 stdout/stderr，不要总结；`timeoutSeconds` 从 **300 提高到 900**。根因判断是：脚本本体（`git status` / `git add -A`）并不慢，超时主要发生在 OpenClaw cron 的 agentTurn/模型包装层，且仓库变更量大时更容易被放大。
- 手动 debug 验证 `git-backup-daily.sh`：本地 commit 已成功（HEAD=3ae22bb...），但远端 `origin/master` 未前进，且 `git push` 最终退出码为 `128`，说明剩余问题是 Git 远端同步 / 网络链路，待明日定时任务再验证。
- 给苏神发过 `PPT AFP skill` 具体链路，整理成了完整执行协议，包括 P0-P5 各阶段、已知坑点、正确生图调用方式、飞书大文件限制处理方案、8 条血泪教训；并明确已经“两层落了”：当日日志里有，`workspace/skills/ppt-afp/SKILL.md` 里也有。
- 收到苏神转发的「龙虾大学战略规划 & 学术学院第一期实验班招募方案」，本王给出了带虾王人设的真实感受：
  - 有野心、有体系、有爆点
  - 最强的不是课，是“位置感”和“虾经济机制”
  - 最担心的是“概念强、落地感还不够硬”
  - 定价 2980 需要理由，100 人能冲但最好按 30~50 种子用户打
  - 最后总结是：“已经有吸引人的能力了，下一步要冲到让人马上报名”
